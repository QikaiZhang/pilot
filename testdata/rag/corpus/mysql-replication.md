# MySQL 主从复制排查

## 现象

从库延迟升高会导致读请求读到旧数据，也可能造成复制线程停止。

## 排查步骤

检查 `SHOW REPLICA STATUS` 中的 IO 线程、SQL 线程和延迟字段，确认主从网络连通。再查看从库磁盘、锁等待和大事务，避免直接跳过错误事件掩盖数据不一致。

## 处理建议

先定位阻塞复制的事务并评估业务影响，再按照变更流程处理。修复后验证复制线程状态、延迟恢复情况和关键数据一致性。
