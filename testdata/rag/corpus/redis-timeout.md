# Redis 连接超时排查

## 现象

应用访问 Redis 时出现连接超时，通常表现为请求延迟升高或连接池耗尽。

## 排查步骤

先检查 Redis 实例是否存活，再检查应用到 Redis 的网络连通性和连接池配置。使用 `redis-cli ping` 验证服务响应，查看慢查询和内存水位，确认没有触发淘汰或阻塞。

## 处理建议

确认连接池的最大连接数与业务并发匹配，设置合理的拨号和读写超时。修改连接池参数后逐步发布，并观察错误率和延迟是否恢复。
